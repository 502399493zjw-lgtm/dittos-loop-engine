{
  "name": "dittos-loop-engine",
  "version": "0.1.0",
  "description": "Standalone Loop Flow engine: command-imperative flow primitives + pluggable agent executor + runtime phase/agent event stream.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "dev": "node --import tsx src/main.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
